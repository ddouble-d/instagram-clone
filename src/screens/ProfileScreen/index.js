import React from "react";
import { View, Text } from "react-native";

const ProfileScreen = () => (
  <View style={{ backgroundColor: "#4a8cff", height: 1500 }}>
    <Text
      style={{
        textAlign: "center",
        marginTop: 250,
        fontSize: 30,
        color: "white",
      }}
    >
      Profile
    </Text>
  </View>
);

export default ProfileScreen;
